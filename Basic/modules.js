const names = require('./1-names');
const sayHi = require('./2-Func');
const data = require('./3-alternative-syntax')
const items = require('./3-alternative-syntax')
console.log(data)
console.log(items)
console.log(names)
// sayHi('lucy')
// sayHi('David')  






// whenever we export something as module.exports , it will always first convert that thing in an object and then export it 